# _headers content for your remote micro-frontend (e.g., in public/_headers)

# Apply CORS headers to all JavaScript and related assets in the 'assets' directory
/assets/*
  Access-Control-Allow-Origin: https://your-host-app-domain.netlify.app
  Access-Control-Allow-Methods: GET, OPTIONS
  Access-Control-Allow-Headers: *
  Access-Control-Max-Age: 86400

# Apply CORS headers specifically to the remoteEntry.js file
# (Adjust the path if your remoteEntry.js is nested differently, e.g., /another-path/remoteEntry.js)
/remoteEntry.js
  Access-Control-Allow-Origin: https://chatsyn.netlify.app
  Access-Control-Allow-Methods: GET, OPTIONS
  Access-Control-Allow-Headers: *
  Access-Control-Max-Age: 86400